import { DeepDiscountShoppingValidators } from './deep-discount-shopping-validators';

describe('DeepDiscountShoppingValidators', () => {
  it('should create an instance', () => {
    expect(new DeepDiscountShoppingValidators()).toBeTruthy();
  });
});
